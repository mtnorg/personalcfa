{
  "_pleaseNote": "Please do not touch this file. It will allow you to update your template in the future.",
  "project": {
    "features": {
      "registeredInAppStore": false,
      "semanticRelease": {{project.features.semanticRelease}},
      {{#if project.features.semanticRelease }}
      "semanticReleaseBranch": "{{project.features.semanticReleaseBranch}}",
      {{/if}}
      "useDependabot": {{project.features.useDependabot}}
    },
    "template": "{{project.template}}",
    "packageManager": "{{project.packageManager}}",
    "devBranchName": "{{project.devBranchName}}",
    "gitHubOrganization": "{{project.gitHubOrganization}}"
  },
  "runtime": {
    "node": "{{runtime.node}}"
  },
  "version": "{{runtime.cli}}"
}
